---
name: github-ops
description: Use for any GitHub operation — creating a repo, opening or checking a pull request, managing issues, watching Actions/CI runs, or cutting a release. Prefer the authenticated `gh` CLI over raw git-remote or REST calls. Covers the common commands and the branch-first / confirm-before-outward-facing guardrails.
---

# GitHub operations (`gh`)

`gh` is installed and authenticated — use it for GitHub-specific work (repos, PRs, issues, Actions, releases).
Plain `git` stays the tool for local history. Commands + guardrails (branch-first PRs, confirm outward-facing
actions):

➡️ **[`Process/playbooks/github-ops.md`](../../../Process/playbooks/github-ops.md)**
