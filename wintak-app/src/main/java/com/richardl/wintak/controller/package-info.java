/**
 * Controller layer: mediates between view and model - commands, menu/toolbar actions, and
 * dialog seams (interfaces around {@code FileChooser}/{@code Alert}) so it stays stub-testable.
 */
package com.richardl.wintak.controller;
