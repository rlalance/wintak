---
name: maven
description: Use when building, testing, or running Wintak with Maven, editing pom.xml, adding/upgrading a dependency or plugin, or diagnosing a build/test failure (surefire ran 0 tests, -Dtest no match, file-in-use on target/, version conflicts). Covers the command set and the Maven gotchas.
---

# Maven (build / test / run / deps)

Follow the canonical playbook — commands, pom conventions (version properties, family version
unification, test scope), and the gotchas (silent 0-test runs, surefire naming, target/ locks):

➡️ **[`Process/playbooks/maven.md`](../../../Process/playbooks/maven.md)**

Single source of truth — edit it there, not here.
