---
name: ui-design
description: Use before any UI/UX work in Wintak — designing a screen or component, working with Claude Design, extracting design tokens, or touching JavaFX CSS/colours/spacing/fonts. Covers the design-system discipline (tokens not literals, looked-up colours, reserved semantic colours, shape/density caps, typography roles) and the Claude Design brief→handoff→token workflow with a ready brief skeleton.
---

# UI design (design system + Claude Design)

**Before any UI work, check `Design/` for new/updated handoffs and reconcile tokens first.** Then follow
the canonical playbook — token discipline in `theme.css` (looked-up colours, no inline `setStyle` hex),
and the brief→handoff→token workflow:

➡️ **[`Process/playbooks/ui-design.md`](../../../Process/playbooks/ui-design.md)**

Single source of truth — edit it there, not here.
